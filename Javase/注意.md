- [ ] 乱码形成的原因：1.读取数据时未读完整个汉字

2.编码和解码时的方式不统一

- [ ] 静态代码块里要用到的集合，要用static 修饰 : static Arraylist<String> list=.....
- [ ] 字节，字符的区别 字符：有1，2，3 a，B，c 黑 马 ...都是字符，普通的123ABCabc 由一个字节组成，中文一般由2-3个字节组成，fileread fileinputStream就是这个区别，fileinputStream读中文不出来（原因就是只能读一个字节，而中文由多个字节组成），用fileread

```
//总结：String 长度 str.length();
//      list  长度  list.size();
//      arr   长度  arr.length;
```

长度--集合用list.size();

数组用arr.length;

字符串用str.length();
### string与char[]转换
String str-->char[] chars:Str.toCharArray();

char[] chars-->String :String.valueof(chars);